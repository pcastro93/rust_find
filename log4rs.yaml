refresh_rate: 30 seconds

appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{h({d(%H:%M:%S.%f)(utc)} [{t}] {l}  - {m} \\({M}:{L}\\) )}{n}"

root:
  level: warn
  appenders:
    - stdout
